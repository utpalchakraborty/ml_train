[project]
name = "sft-qwen"
version = "0.1.0"
requires-python = ">=3.12.0, < 3.13"
dependencies = [
  "accelerate>=1.13.0",
  "datasets>=4.6.1",
  "peft>=0.18.1",
  "torch>=2.10.0",
  "transformers>=5.3.0",
  "trl>=0.29.0",
  "wandb>=0.25.0",
]
